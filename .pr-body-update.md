See latest commits on the branch for the full changelog. Summary: auth fixes, safeApiCall consolidation, dead code removal, TypeScript fixes, and bug fixes across Lists 1-4.

## Latest changes (this session)

### Refactors (List 3: Models, Agents, Skills, Tools, Personalities)
- remove unused PERSONALITIES/PersonalityName from personalities.ts — defined but never consumed anywhere in the codebase
- inline formatApiError() into apiFetch() — eliminated unnecessary function indirection
- update api-fetch-format test to test apiFetch error-throwing behavior (was testing the now-removed formatApiError function directly)

All 632 tests pass. Zero TypeScript errors.

### Prior session changes (List 4: Models, HERMES.md, Environment, All Settings)
- remove dead updateFallbackConfig() from fallbacks-repository — only used internally by batch, inlined directly
- remove redundant syncSingleCredentialToHermesEnv() wrapper
- fix duplicate icon: code_execution (changed from Terminal to Cpu)
- fix duplicate icon: streaming (changed from Activity to Globe)
- fix unused multi-line import in fallback sync route

### Prior session changes (List 2: Cron, Missions, Chat)
- Cron page: wrap async onPauseAll handler, JSX structure fix
- Consolidate raw fetch() to safeApiCall across Cron, Missions, and Chat

### Prior session changes (List 1: Dashboard, Sessions, Memory, Logs)
- Auth gap fixes, session API improvements, dead code removal
