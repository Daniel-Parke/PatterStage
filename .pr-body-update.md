See latest commits on the branch for the full changelog. Summary: auth fixes, safeApiCall consolidation, dead code removal, TypeScript fixes, and bug fixes across Lists 1-4.

## Latest changes (this session)

### Refactors (List 2: Cron, Missions, Chat)
- **useMissionsPage.ts**: `dispatchPayload` — wrap in `useCallback` with proper deps (was recreated on every render, causing unnecessary re-renders of `handleCreate` and all inline call sites); `resetForm` — wrap in `useCallback([])` (same reason)
- **chat/page.tsx**: `handleKeyDown` — add `void handleSend()` to discard the async result

All 630 tests pass. Zero TypeScript errors.

### Prior session changes

#### List 4: Models, HERMES.md, Environment, All Settings
- **useModelsPage.ts**: `handleSyncFallbackToHermes` — fix missing `setSyncingFallback(false)` in catch block; replace `safeApiCall`+manual JSON.stringify with `apiFetch`; add missing `flushFallbackConfigSave` to `useCallback` deps
- **fallback-sync-helpers.ts**: return explicit `SyncResult | null` interface instead of `void`
- **hermes-config-sync.ts**: add null guard for OAuth-only providers with no `.env` key
- **fallbacks-repository.ts**: clarify enabled ternary readability
- **models-repository.ts**: `upsertModel` throws on invalid task types instead of silent no-op

#### List 2: Cron, Missions, Chat (prior session)
- **useMissionsPage.ts**: Replace `safeApiCall` with `apiFetch` for profile skills/toolsets loading; remove dead `createFormRef`
- **useCronJobs.ts**: `handlePauseAll` — remove unnecessary async IIFE wrapper
- **useSystemCronJobs.ts**: `handlePauseAll` — move `loadJobs()` outside conditional so it always runs (bug fix)
- **cron/page.tsx**: `onPauseAll` — replace async IIFE with `void ... .finally(...)`
- **chat/route.ts**: remove dead `nonStreaming` variable

#### List 1: Dashboard, Sessions, Memory, Logs
- Auth gap fixes, session API improvements, dead code removal

#### Prior refactors
- remove dead `updateFallbackConfig()` from fallbacks-repository
- remove redundant `syncSingleCredentialToHermesEnv()` wrapper
- fix duplicate icons: code_execution (Terminal→Cpu), streaming (Activity→Globe)
- fix unused multi-line import in fallback sync route
