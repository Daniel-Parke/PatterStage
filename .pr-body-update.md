See latest commits on the branch for the full changelog. Summary: auth fixes, safeApiCall consolidation, dead code removal, TypeScript fixes, and bug fixes across Lists 1-4.

## Latest changes (this session)

### Refactors (List 4: Models, HERMES.md, Environment, All Settings)
- **useModelsPage.ts**: `handleSyncFallbackToHermes` — fix missing `setSyncingFallback(false)` in catch block (spinner left spinning on failure); replace `safeApiCall`+manual JSON.stringify with `apiFetch` for consistency with all other handlers; add missing `flushFallbackConfigSave` to `useCallback` dependency array (stale closure bug)
- **fallback-sync-helpers.ts**: `syncEnabledFallbackChainToHermes` — return `SyncResult | null` with explicit interface (`hermesHome`, `configPath`, `backupPath`) instead of `void`; callers that need the backup path now receive it, empty-chain case returns `null`
- **hermes-config-sync.ts**: `removeCredentialFromHermesEnv` — add explicit null guard for OAuth-only providers (e.g. nous) that have no `.env` key to delete; avoids passing `null` to `next.delete()`
- **fallbacks-repository.ts**: `addFallbackEntry` — clarify enabled ternary from `=== false ? 0 : 1` to `!== false ? 1 : 0` (identical logic, reads as "default true unless explicitly false")
- **models-repository.ts**: `upsertModel` — replace silent no-op for invalid task types with upfront validation that throws; `upsertModel` is called from internal code (hermes-import), not user input, so invalid task types are programmer errors that should surface loudly rather than silently disappearing

All 632 tests pass. Zero TypeScript errors.

### Prior session changes

#### List 2: Cron, Missions, Chat
- **useMissionsPage.ts**: Replace `safeApiCall` with `apiFetch` for profile skills/toolsets loading; filter out `AbortError` from console.warn; remove dead `createFormRef` (never attached to any DOM element)
- **useCronJobs.ts**: `handlePauseAll` — remove unnecessary async IIFE wrapper
- **useSystemCronJobs.ts**: `handlePauseAll` — move `loadJobs()` call outside conditional so it always runs (bug fix)
- **cron/page.tsx**: `onPauseAll` — replace 4-line async IIFE with `void agent.handlePauseAll().finally(...)`
- **chat/route.ts**: remove dead `nonStreaming` variable

#### List 1: Dashboard, Sessions, Memory, Logs
- Auth gap fixes, session API improvements, dead code removal

#### Prior refactors (Lists 1-4)
- remove dead `updateFallbackConfig()` from fallbacks-repository
- remove redundant `syncSingleCredentialToHermesEnv()` wrapper
- fix duplicate icon: code_execution (changed from Terminal to Cpu)
- fix duplicate icon: streaming (changed from Activity to Globe)
- fix unused multi-line import in fallback sync route
