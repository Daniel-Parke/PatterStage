See latest commits on the branch for the full changelog. Summary: auth fixes, safeApiCall consolidation, dead code removal, TypeScript fixes, and bug fixes across Lists 1-4.

## Latest changes (this session)

### Review findings (List 2: Cron, Missions, Chat) — 2026-05-28 [FULL REVIEW]
Comprehensive review of all source files (missions/route.ts 551 lines, hermes-sync.ts 580 lines, useMissionsPage.ts 1181 lines, cron/page.tsx 398 lines, chat/page.tsx 570 lines, cron/route.ts 471 lines, etc.).

**Codebase is in excellent shape** — issues found and fixed:
- All 110 List 2 tests pass, zero TypeScript errors
- `pauseAll` patterns verified consistent across all 4 affected files
- `nonStreaming` dead code removal confirmed already done
- `handlePauseAll` in `useSystemCronJobs.ts` correctly calls `loadJobs()` unconditionally
- `repeat` field normalization in `cron/write.ts` handles both boolean and object forms correctly
- `resolveMissionId` helper correctly handles both `id` and `missionId` body keys
- `streamChatResponse` and `readChatStream` used exclusively in `chat/page.tsx` — no dead exports

**Issues fixed this session:**
- `cron/route.ts`: `normalizeRepeat` was imported from wrong module (`hermes-sync`) — correct source is `cron/write.ts`
- `cron/hermes-sync.ts`: hardcoded `GATEWAY_BASE = "http://127.0.0.1:8642"` replaced with `gatewayUrl()` from `gateway-client`
- `cron/write.ts`: `parseScheduleToJson` had unnecessary `typeof` guards — `parseSchedule` always returns `ParsedSchedule` object
- Deleted dead barrel file `src/lib/cron-display.ts` (0 consumers, pure re-export of `schedule/types`)
- `cron/page.tsx`: removed awkward `void loadHardwareJobs;` hack — `hardware.loadJobs()` called directly in useEffect
- `MissionEditorPanel.tsx`: fixed split `lucide-react` import that caused `ChevronRight` to not be found by TypeScript

### Prior session changes

#### List 2: Cron, Missions, Chat (prior session)
- remove dead `updateFallbackConfig()` from fallbacks-repository
- remove redundant `syncSingleCredentialToHermesEnv()` wrapper
- fix duplicate icon: code_execution (changed from Terminal to Cpu)
- fix duplicate icon: streaming (changed from Activity to Globe)
- fix unused multi-line import in fallback sync route

#### List 2: Cron, Missions, Chat
- Cron page: wrap async onPauseAll handler, JSX structure fix
- Consolidate raw fetch() to safeApiCall across Cron, Missions, and Chat

#### List 1: Dashboard, Sessions, Memory, Logs
- Auth gap fixes, session API improvements, dead code removal
